写下它们真正的功能作用，将每一种关键字和符号用在程序里，可以
用一个小程序来做，尽量多写些程序来巩固记忆。
一，关键字
and 与
关键字
del 删除内容
from 从...库里提取
not 非
while 
as 
elif 其他如果
global 
or 或
with
assert 插入
else 其他
if 如果
pass 这一项退出
yield
break 打断
except 除了
import 导入
print 打印
class 类
exec 
in 
raise 
continue 
finally
is
return 返回值
def 定义函数
for 循环
lambda
try

数据类型
True
False
None
strings
numbers
floats
lists

字符串转义序列
   \\                           反斜杠（\）
   \'                           单引号（'）
   \"                           双引号（"）
   \a                           ASCII响铃符（BEL）
   \b                           ASCII退格符（BS）
   \f                           ASCII进纸符（FF）
   \n                           ASCII换行符（LF）
   \r                           ASCII回车符（CR）
   \t                           ASCII水平制表符（TAB）
   \v                           ASCII 垂直制表符（VT）

 
 
 字符串格式化
  
 


